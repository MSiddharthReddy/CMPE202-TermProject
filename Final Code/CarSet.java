/**
 * Write a description of class CarFactory here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class CarSet extends VehicleOverhead
{
    // instance variables - replace the example below with your own
    public CarSet(int a){
        setPrice(2);
        setTime(a);
    }
    
}
